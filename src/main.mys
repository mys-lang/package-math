from math import sin
from math import cos

def main():
    for angle in range(-1.5, 1.75, 0.25):
        print(f'sin({angle}) =', sin(angle))

    print()

    for angle in range(-1.5, 1.75, 0.25):
        print(f'cos({angle}) =', cos(angle))
